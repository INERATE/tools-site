"use client";

import AiObjectEraserPage from "../ai-object-eraser/page";

export default function WatermarkRemoverPage() {
  return (
    <AiObjectEraserPage
      title="Remove Watermarks from Photos"
      blurb="Brush over a watermark, stamp, date tag, or logo and it is rebuilt from the surrounding image — no blur, no smudge. Runs entirely in your browser, so the photo never leaves your device."
    />
  );
}
